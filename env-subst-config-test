
provider_name: $PROVIDER_NAME
provider_version: $PROVIDER_VERSION

provider_config: $PROVIDER_CONFIG_NAME
project_id: $PROVIDER_PROJECT_ID

bucket: $BUCKET_NAME

network: $NETWORK_NAME
subnet: $NETWORK_SUBNET_NAME

k8s: $KUBERENTES_CLUSTER_NAME
workers: $KUBERENTES_WORKER_NODE_NAME
k8s_network: $KUBERENTES_CLUSTER_NETWORK
k8s_subnet: $KUBERENTES_CLUSTER_SUBNET
